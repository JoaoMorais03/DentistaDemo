import SwiftUI

struct ColorTheme {
    static let primary = Color(red: 0.0, green: 0.5, blue: 0.8) // Calming blue
    static let secondary = Color(red: 0.1, green: 0.6, blue: 0.9) // Lighter blue
    static let accent = Color(red: 0.0, green: 0.7, blue: 0.7) // Teal accent
    static let background = Color(red: 0.95, green: 0.97, blue: 1.0) // Very light blue/white
    static let text = Color(red: 0.1, green: 0.1, blue: 0.2) // Dark blue-black
    static let textSecondary = Color(red: 0.4, green: 0.4, blue: 0.5) // Gray blue
    static let success = Color(red: 0.0, green: 0.7, blue: 0.4) // Green
    static let warning = Color(red: 0.9, green: 0.6, blue: 0.0) // Orange
    static let error = Color(red: 0.9, green: 0.2, blue: 0.2) // Red
    static let headerBackground = Color(red: 0.85, green: 0.95, blue: 1.0) // Light blue for headers
} 